#include <stdio.h> //Fazer com que o usuário entenda o que está acontecendo, como operações de entrada e saída de dados.

int main(void)
{
    printf("Hello, World!\n"); // Mostra a mensagem "Hello, World!" para o usuário, quando encontra o \n, o programa pula de linha.
    return 0;
}